/**
 * 라이트 버전의 경로 정본.
 *
 * 본 MVP 는 `tabs.ts` 에 4탭(오늘·흐름·병원·아이)을 두고 하단 탭바로
 * 오갔습니다. **라이트에는 탭바가 없습니다** — 흐름·병원 탭은 자리표시자였고
 * 아이 탭은 백업 링크 한 줄뿐이라 탭으로 승격할 내용이 없었습니다.
 * 그래서 화면 사이의 이동이 전부 "다음"으로만 흐르고, 경로도 목록이 아니라
 * 상수 몇 개면 됩니다.
 *
 * 문자열을 여기 모아 둔 이유는 진입 관문(`RootGate`)과 화면들이 같은 주소를
 * 두 번 적지 않게 하기 위해서입니다. 오타가 나면 조용히 리다이렉트 루프가
 * 됩니다.
 */

/** 인트로 3장. 처음 앱을 연 사람이 보는 첫 화면. */
export const INTRO_PATH = '/intro'

/** 아이 등록. 인트로를 지나면 여기로 옵니다. */
export const PET_NEW_PATH = '/pet/new'

/** 온보딩 마무리 1장. 등록 직후 한 번만 지나갑니다. */
export const WELCOME_PATH = '/welcome'

/** 홈. 온보딩을 끝낸 사람이 앱을 열면 늘 여기입니다. */
export const HOME_PATH = '/today'

/** 내보내기·가져오기. 홈 하단 링크로만 들어갑니다. */
export const BACKUP_PATH = '/pet/backup'
